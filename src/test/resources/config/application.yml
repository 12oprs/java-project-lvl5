base-url: ""

spring:
  application:
    name: app
  datasource:
    url: jdbc:h2:mem:db
    username: sa
    password:
  jpa:
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yml
#  mvc:
 #   hiddenmethod:
  #    filter:
   #     enabled: true
logging:
  level:
    root: INFO